{
  "name": "stayvia",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=@stayvia/web",
    "dev:api": "npm run dev --workspace=@stayvia/api",
    "dev:marketing": "npm run dev --workspace=@stayvia/marketing",
    "build:web": "npm run build --workspace=@stayvia/web",
    "build:api": "npm run build --workspace=@stayvia/api",
    "build:marketing": "npm run build --workspace=@stayvia/marketing",
    "db:generate": "npm run db:generate --workspace=@stayvia/api",
    "db:seed": "npm run db:seed --workspace=@stayvia/api",
    "lint": "eslint apps packages",
    "lint:strict": "eslint apps packages --max-warnings 0",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:debug": "playwright test --debug"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@material-symbols/svg-400": "^0.45.8",
    "@playwright/test": "^1.61.1",
    "eslint": "^9.39.4",
    "eslint-plugin-react-hooks": "^5.2.0",
    "globals": "^17.6.0",
    "typescript-eslint": "^8.60.1"
  }
}
